# High Win Rate - Admin Panel

پنل مدیریت فروشگاه استراتژی‌های معاملاتی با Next.js

## 🚀 راه‌اندازی سریع

### روش سریع (پیشنهادی) ⭐

```bash
./start-dev.sh
```

این اسکریپت به صورت خودکار:
- بررسی وجود Node.js و npm
- ایجاد فایل `.env` از `env.example.txt` (در صورت نیاز)
- نصب dependencies
- راه‌اندازی سرور توسعه

### روش دستی

1. **نصب dependencies**
```bash
npm install
```

2. **تنظیم متغیرهای محیطی**
```bash
cp env.example.txt .env
# ویرایش فایل .env در صورت نیاز
```

3. **راه‌اندازی سرور توسعه**
```bash
npm run dev
```

پنل ادمین در آدرس `http://localhost:3002` در دسترس خواهد بود.

## 📋 قابلیت‌ها

### صفحات اصلی

- **صفحه اصلی** (`/`) - ریدایرکت به داشبورد یا لاگین
- **ورود** (`/login`) - ورود به پنل مدیریت
- **داشبورد** (`/dashboard`) - نمایش آمار کلی سیستم
- **مدیریت محصولات** (`/admin/products`) - CRUD محصولات
- **مدیریت دوره‌ها** (`/admin/courses`) - CRUD دوره‌ها
- **مدیریت کاربران** (`/admin/users`) - مدیریت کاربران
- **مدیریت تراکنش‌ها** (`/admin/transactions`) - مشاهده تراکنش‌ها
- **مدیریت کدهای تخفیف** (`/admin/discounts`) - CRUD کدهای تخفیف
- **مدیریت فایل‌ها** (`/admin/files`) - آپلود و مدیریت فایل‌ها

### ویژگی‌ها

- ✅ احراز هویت کامل (ورود)
- ✅ کنترل دسترسی بر اساس نقش (فقط Admin)
- ✅ مدیریت کامل محصولات
- ✅ مدیریت کامل دوره‌ها
- ✅ مدیریت کاربران
- ✅ مشاهده تراکنش‌ها
- ✅ مدیریت کدهای تخفیف
- ✅ آپلود و مدیریت فایل‌ها
- ✅ UI/UX مدرن با Tailwind CSS

## 🛠️ تکنولوژی‌ها

- **Next.js 16** - Framework React
- **TypeScript** - Type Safety
- **Tailwind CSS** - Styling
- **React Context** - State Management

## 📁 ساختار پروژه

```
admin-panel/
├── app/                    # صفحات Next.js (App Router)
│   ├── page.tsx           # صفحه اصلی
│   ├── layout.tsx         # Layout اصلی
│   ├── login/             # صفحه ورود
│   │   └── page.tsx
│   ├── dashboard/         # داشبورد مدیریت
│   │   └── page.tsx
│   └── admin/             # صفحات مدیریت
│       ├── products/
│       ├── courses/
│       ├── users/
│       ├── transactions/
│       ├── discounts/
│       └── files/
├── components/            # کامپوننت‌های React
│   ├── ui/               # کامپوننت‌های UI پایه
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   └── Card.tsx
│   └── AdminNavbar.tsx   # نوار ناوبری ادمین
├── contexts/              # React Contexts
│   └── AuthContext.tsx   # مدیریت احراز هویت
├── lib/                   # کتابخانه‌ها و utilities
│   └── api.ts            # API Client برای ادمین
└── README.md             # مستندات پنل ادمین
```

## 🔐 امنیت

- فقط کاربران با نقش `admin` می‌توانند به پنل دسترسی داشته باشند
- تمام درخواست‌ها با JWT Token محافظت می‌شوند
- در صورت عدم دسترسی، کاربر به صفحه لاگین هدایت می‌شود

## 🔗 لینک‌های مرتبط

- Backend API: `http://localhost:3000`
- Web App (Client): `http://localhost:3001`
- Admin Panel: `http://localhost:3002`

---

**نکته**: قبل از راه‌اندازی پنل ادمین، مطمئن شوید که بک‌اند در حال اجرا است.

